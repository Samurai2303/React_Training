let baseUrl = 'https://jsonplaceholder.typicode.com';

let urls = {
    users: '/users',
    posts: '/posts',
    comments: '/comments',
    albums: '/albums',
    photos: '/photos',
    todos: '/todos'
}

export {baseUrl, urls};